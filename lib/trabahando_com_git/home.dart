import 'package:flutter/material.dart';

class TrabalhandoComGit extends StatelessWidget {
  const TrabalhandoComGit({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
